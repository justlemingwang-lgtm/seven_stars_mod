package com.example.examplemod.compat.top;

public class TopQiCompat {
    private TopQiCompat() {
    }

    // TODO: Add a The One Probe plugin guarded by ModList.get().isLoaded("theoneprobe") and append "气：current / max" for LivingEntity targets.
}
