package com.example.examplemod.compat.jade;

public class JadeQiCompat {
    private JadeQiCompat() {
    }

    // TODO: Add a Jade plugin guarded by ModList.get().isLoaded("jade") and append "气：current / max" for LivingEntity targets.
}
