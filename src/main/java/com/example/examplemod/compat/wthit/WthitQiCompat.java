package com.example.examplemod.compat.wthit;

public class WthitQiCompat {
    private WthitQiCompat() {
    }

    // TODO: Add a WTHIT plugin guarded by ModList.get().isLoaded("wthit") and append "气：current / max" for LivingEntity targets.
}
